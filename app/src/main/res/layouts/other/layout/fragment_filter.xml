<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/filter_screen"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <android.support.v4.view.ViewPager
        android:id="@+id/pager"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
    </android.support.v4.view.ViewPager>

    <com.astuetz.PagerSlidingTabStrip
        android:id="@+id/tabs"
        android:layout_width="match_parent"
        android:layout_height="@dimen/pager_height"
        android:background="?attr/colorPrimary"
        app:pstsIndicatorColor="@color/banana"
        app:pstsTabPaddingLeftRight="25dp"
        app:pstsTabTextFontFamily="sans-serif"
        app:pstsTabTextStyle="bold"
        app:pstsTabTextAllCaps="true"
        app:pstsTabTextColor="@color/toolbar_text_clr"
        app:pstsTabTextSize="14sp" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:text="ПРИНЯТЬ ФИЛЬТРЫ"
        android:id="@+id/accept_filters"
        android:layout_alignParentBottom="true"
        android:background="@color/banana_accent"
        android:gravity="center"
        android:textColor="@color/textColorPrimaryDark"
        android:alpha="0.8" />

</RelativeLayout>